worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    # Upstream to the FastAPI service in docker-compose
    upstream api_backend {
        server api:8000;
    }

    server {
        listen 80;
        server_name _;

        # Forward all requests to the FastAPI app
        location / {
            proxy_pass         http://api_backend;
            proxy_http_version 1.1;

            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;

            # Upgrade headers for websockets and streaming (if added later)
            proxy_set_header   Upgrade $http_upgrade;
            proxy_set_header   Connection "upgrade";
        }

        # Simple health endpoint for load balancers / uptime checks
        location /health {
            proxy_pass http://api_backend/health;
        }
    }
}

